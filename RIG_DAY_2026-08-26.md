# Rig day — 2026-08-26 (surgical-suite move + validation; SURGERY IS TOMORROW)

The experiment moved up: the acute experiment is **tomorrow, Thu 2026-08-27** (was
Fri 08-28). Today's job is to bank every gate that tomorrow depends on, in the new
room, plus validate the NEW `nThw` touch pulse end to end. Tomorrow's runbook is
`RIG_DAY_2026-08-27.md` — everything below feeds it.

A successful day: every PHASE-A check green, Wav1/Wav2 saving verified ON, the
artifact/2x decisions made, TWO thwack test blocks extracted with `nThw`, the
100-repeat tape dress-rehearsed in sim, both repos pushed.

---

## Quick reference — the whole day

```powershell
# Terminal 1 (analysis/loop)                    # Terminal 2 (servers, MATLAB)
.\rig\0_preflight.ps1
.\rig\6_udp_selftest.ps1 -RZ2 10.1.0.100
# quiet capture (recording only, no loop needed)
                                                .\rig\1c_server.ps1 -Run rndval -Schedule random -Ticks 28000
.\rig\2_loop.ps1 -Run rndval -RZ2 10.1.0.100 -TimeoutMs 10 -Ticks 28000 -TickFrames 6
python rig\check_impulse_delivery.py --design design_runrndval.csv --block <block>
python rig\assess_artifact.py --block <block>
# 2 thwack test blocks (Synapse only), then per block:
python <NNC>\scripts\touch\extract_nthw_templates.py --block <block> --site D1 --day 2026-08-26 --force
```

`python` = `C:\Users\brets\Documents\Repositories\PythonIntanAnalysis\.venv\Scripts\python.exe`
(bare `python` on a fresh shell is the MS-Store stub). `<NNC>` = `C:\Users\brets\Documents\Repositories\NNController`.

---

# PHASE A — Preflight after the move

**Status: RUN 2026-08-26 (by Claude, from this PC). Results below — re-run any
line if hardware is re-cabled.**

| # | Check | Command | Result 2026-08-26 |
|---|-------|---------|-------------------|
| A1 | Link up, speed | `Get-NetAdapter` | **PASS** — Ethernet Up. Still **10 Mbps** (known suspect cable; fine for control traffic, swap opportunistically) |
| A2 | Static IP survived move | `ipconfig` | **PASS** — Ethernet 10.1.0.1/24 (manual, persisted) |
| A3 | Route/ARP/firewall | `.\rig\net_diag.ps1` | **PASS** — on-link via Ethernet, RZ2 answers ICMP+ARP (00-04-A3-00-00-00) |
| A4 | RZ2 speaks protocol | `python rig\find_rz2.py --hosts 10.1.0.100` | **PASS** — VALID ACK, protocol_version=1 |
| A5 | Toolchain smoke | `.\rig\0_preflight.ps1` | **PASS** — 300/300, dropped=0, window 6/6/0; AllModels backed up → `AllModels_backup_20260826.mat` |
| A6 | MATLAB license | `matlab -batch` | **PASS** (bench suite ran) |
| A7 | UDP selftest | `.\rig\6_udp_selftest.ps1 -RZ2 10.1.0.100` | **PASS** — A/B1/B2/B3/C/D/E1 all pass; E2 (live data packet) deliberately skipped — run `-LiveStim` tomorrow pre-animal |
| A8 | Bench suite | `matlab -batch bench_test_reference_mpc` | **19/22** — the 3 fails are `reference steers u` / `preview anticipates` / `exact zero at zero target`, the KNOWN false-fail from the zero-gain saline junk fit at AllModels(10). Expect 22/22 after tomorrow's real `3_fit -Save`. NOT a regression |
| A9 | cpp selftest | `.\cpp_controller.exe --selftest` | **PASS** (0 failures) |
| A10 | Choi tests | `python rig\test_choi_synthesis.py` | **ALL PASS** |
| A11 | PageHeap | `.\rig\pageheap.ps1` | **OFF** (correct for timing runs) |
| A12 | WER dumps | registry + write test | **PASS** — full dumps, keep 16, `crash_dumps\` writable |
| A13 | Disk space | `Get-PSDrive C` | **396 GB free** — plenty for ~42 recordings |
| A14 | Repo pushed | `git status` | **PASS** — pushed `0a082e6..77bdd9b` today; push again end of day |

Remaining physical checks (cannot be done from software — do on arrival):
- **PO8e fiber reseated** after transport. A half-seated fiber passes GET_VERSION
  but drops frames — the first live run of PHASE B catches it (`Waiting for
  stream...` forever, or offset jumps).
- **Stimulator on charge NOW** and again overnight (dead battery = the 08-14
  false-null trap; check charge before interpreting ANY null).
- Both PCs: sleep/power-save OFF.

### → A-fail. Ethernet has no 10.1.0.1
`.\rig\set_rig_ip.ps1` (dry run), then `-Apply` from an admin shell.

---

# PHASE B — Saline gates (in the suite, in this order)

## B1. Quiet capture (~60 s, no stim, no loop)
Record a Synapse block with everything powered, nothing commanded.
- **FIRST verify Wav1/Wav2 DISK SAVING is enabled in Synapse** — they streamed
  but did not SAVE in every 08-18 block. Open the fresh block and confirm Wav1
  is nonzero before doing anything else.
- Bank: noise floor per channel, feature baseline (mean 6-sample MAV, volts) —
  this is the suite-environment `--baseline` rehearsal value — and the 60 Hz +
  harmonics fraction (new room = new line-noise environment; decision rule:
  quantify FIRST, notch fit-side only if it dominates).

## B2. Artifact retest at high amplitude (standing pre-deployment gate)
The 08-18 bath artifacts were only 1.2-1.3x the floor — user suspects the bath
arrays. Retest here with better coupling / higher amp (saline has no tissue
limit; uMax 25-40):
```powershell
# Terminal 2:                                   # Terminal 1 after 'ready':
.\rig\1c_server.ps1 -Run art1 -Ticks 7000       .\rig\2_loop.ps1 -Run art1 -RZ2 10.1.0.100 -TimeoutMs 10 -Ticks 7000 -TickFrames 6
# recording LAST, then 'go'
python rig\assess_artifact.py --block <block>
```
Verdict drives the `--feature-trim` decision for tomorrow (default stays OFF).

## B3. Randomized-probe delivery validation (the Friday→Thursday tissue protocol)
```powershell
.\rig\1c_server.ps1 -Run rndval -Schedule random -PulseAmps 2,4,6,9,13,18,25 -GapTicks 36 -GapJitterMs 60 -Ticks 28000
.\rig\2_loop.ps1 -Run rndval -RZ2 10.1.0.100 -TimeoutMs 10 -Ticks 28000 -TickFrames 6
python rig\check_impulse_delivery.py --design design_runrndval.csv --block <block>
```
Expect: wire==design, ~0 missed/doubled. (Short 28000-tick version of tomorrow's
183000-tick run — validates the schedule + this room, not the trial counts.)

## B4. 2x-rate gate (ONLY if the Synapse circuit change is in)
Positive: `.\rig\2_loop.ps1 -Run x2 -RZ2 10.1.0.100 -StreamFs 1220.703125 -TickFrames 12 -FeatureWindow 12 -Ticks 7000` →
offset step=20, no mismatch warning, PLL stats comparable, delivery audit clean.
Negative: same run WITHOUT `-StreamFs` → the mismatch warning MUST fire.
**Tomorrow uses 2x only on a FULL pass of both; otherwise the untouched 610 path.**

## B5. Closed-loop dress rehearsal (banks the suite saline timing number)
Probe capture → `3_fit` (check the printed Ts) → `4_mpc_server -Reference
ref_steps.csv -RWeight 1e-3 -Pairs 1 -FeatureChannel 1` + `2_loop -TickFrames 6`
→ `python rig\timing_check.py` + `python rig\tracking_metrics.py`.

---

# PHASE C — nThw validation (the new touch pulse)

Record TWO test thwack blocks with the thwacker + nThw digital input:
- **Block 1**: ~20+ normal touches (any surface).
- **Block 2**: mix of touches AND **no-contact swings** — proves nThw fires
  without contact, which tomorrow's SHAM site depends on.

After EACH block, immediately:
```powershell
python <NNC>\scripts\touch\extract_nthw_templates.py --block <block dir> --site D1 --day 2026-08-26 --force
```
(`--force` because saline templates are noise — the pipeline must still run;
`--site D1` is a placeholder label today.)

PASS =
- `nThw` appears in the epocs list; onset count == what you did by hand;
- contact durations are real (NOT flagged "gap-filled" — that warning means the
  falling edge is not wired);
- `mPos` agreement high if the manipulandum also ran;
- Wav1 gate passes (proves B1's saving fix);
- npz + meta land in `<NNC>\outputs\BiomimeticInversion\touch\Acute_2026-08-26\`.

Already verified off-rig today (no hardware): mpos-regression mode reproduces
the legacy `Acute_121223` template **byte-identically** (shc 0.977, peak 400 µV);
thwk epoc mode agrees 150/150 with the mPos detector.

### → C-fail. `nThw` not in the epocs list
The store is not enabled/named in Synapse. Fix there; nothing code-side can help.

---

# PHASE D — Tonight (off-rig)

1. **100-repeat tape dress rehearsal in sim — DONE 2026-08-26, PASS.**
   `4_mpc_server -Reference <22,200-tick tape> -ControlHorizon 20` +
   `2_loop -Sim -Ticks 22200 -TickFrames 6`: **22,200/22,200, dropped=0, window
   6/6/0, PLL resyncs 0, freshTicks 98.8%**, server median turnaround 1.8 ms.
   Ledger: 3 attempts — attempt 1 failed AT STARTUP (replies=0 from tick 1,
   loop-side recv failure; server itself serviced 17,548 packets cleanly),
   attempts 2-3 healthy. **Guard adopted into tomorrow's runbook: a
   `policy=fresh` line must appear by tick ~100 or abort + relaunch the loop —
   the failure mode is total and visible within seconds.**
2. `prepare_arm_runs.py` smoke against today's test templates.
3. Re-check stimulator charging for tomorrow.
4. `git push` both repos (TDTClosedLoop code + NNController template artifacts).

---

*Standing rules (unchanged): server ready → loop at 'go' → START RECORDING →
type 'go'; kill stale MATLAB between servers; blocks nest one directory level;
leave `-TickPhaseUs` unset; after ANY crash `python rig\send_envelope.py --kind
const --umax 0`.*
