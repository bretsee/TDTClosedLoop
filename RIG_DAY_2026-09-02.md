# Rig day — 2026-09-02 (WEDNESDAY, re-setup + 64-ch/MIMO validation; NO SALINE)

Goal: reassemble the rig after the 09-01 teardown, bring up **64-ch recording
(2×32 headstages, 4 PZ2 banks, one 64-wide Wav1)** and validate the **MIMO +
cpp-only control stack** for Thursday's acute. **No saline bath** (expensive
arrays stay dry): every stim gate is wire-level only; PHYSICAL delivery to
tissue is validated by Thursday's first in-vivo probe block — that is the
day's top accepted risk, written here so nobody mistakes wire-green for
tissue-green.

Standing rules: shells need `Set-ExecutionPolicy -Scope Process Bypass` +
`.\rig\0_preflight.ps1` (PATH). NEVER run Synapse rig **Detect** (recovery =
Backups `.synrig` import). After ANY channel-width edit: rebuild → banner →
store-width check (the 08-29 stale-apply lesson). Apps closed before every
run. `python` = the PythonIntanAnalysis venv.

## S0 — PC + toolchain (no hardware)
```powershell
Get-PnpDevice | ? { $_.InstanceId -match 'VEN_4550' } | Format-List FriendlyName, Status, Problem
.\rig\0_preflight.ps1 -InputChannels 64      # PASS, dropped=0, exact(64/64) in sim
.\cpp_controller.exe --selftest              # 13/13
# MATLAB fallback: matlab -batch bench_test_reference_mpc   -> 28/28
```

## S1 — Reassembly + Synapse (user)
1. Cable per the 09-01 teardown photos; both 32-ch headstages into banks 1+2
   and 3+4 (channel ranges 1-32 / 33-64 — verify bank labels, the 08-30
   lesson: channel range is fixed per connector).
2. Synapse: import `rig_20260901_pre-teardown.synrig` if the rig looks wrong;
   otherwise open the 32-ch experiment, Save-As `*_64ch_20260902`, and edit
   widths manually: PZ2 Channels 64, PO8e tap 64, Wav1 store 64-wide, saving
   ON. Rebuild.
3. **Export a fresh dated `.synrig` immediately after it works.**

## S2 — 64-ch acquisition gates (dry; headstages in air = line noise, that's fine)
```powershell
# probe (Synapse Preview running):
.\MpcPo8eUdpClosedLoop.exe 10.1.0.100 . 64 --controller constant --constant-output 0 --skip-udp-send --max-control-ticks 300
# want: Streaming. numChannels=64 sampleBytes=4 (float32), channelMode=exact(in=64 card=64)
```
Then a 60 s recording →
```powershell
python rig\ingest_block.py --label "64ch dry quiet" --mode thwack   # gallery will show line noise; that's the point
python rig\analyze_quiet_capture.py --block <blk>                   # Wav1 (64, T), all 64 nonzero
```
PASS: banner 64/exact; Wav1 `(64, T)` with **both banks live 1-64** (the
08-30 zeros-above-N failure mode is what this catches).

## S3 — loop soak at 64
```powershell
.\cpp_controller.exe --mode openloop --exc-kind prbs --umin 0 --umax 0 --capture capture_rig_runsoak64.csv --max-packets 6000
.\rig\2_loop.ps1 -Run soak64 -RZ2 10.1.0.100 -InputChannels 64 -TickFrames 6 -Ticks 6000 -TimeoutMs 10
```
PASS: 6000/6000, dropped=0, window 6/6/0, capture header `y1..y64`, feature
cost still holds the tick (watch totalTick in the summary).

## S4 — wire-level stim re-bank (the circuit changed → 08-31 gates are void)
```powershell
.\rig\1c_server.ps1 -Run rndw -Schedule random -Ticks 28000 -UMax 30
.\rig\2_loop.ps1 -Run rndw -RZ2 10.1.0.100 -InputChannels 64 -TickFrames 6 -Ticks 28000 -TimeoutMs 10
# recording LAST, stim enable ON at 'go' (outputs go to the IZ2 with NO array
# attached, or attached per the user's dry-connect judgment)
python rig\check_impulse_delivery.py --block <blk> --capture capture_rig_runrndw.csv --amps 5 10 18 25
```
PASS: wire == design 100%, pair map exact 8/8, carrier base/240 at 6.000
samples/period, UDP1→Scle delay sane. (No tissue: pulses-per-probe audit
still validates the carrier latch path.)

## S5 — MIMO closed-loop dress rehearsal (sim, then live-wire)
```powershell
# sim end-to-end on the real stacked model:
python training\closed_loop_sim.py --plant plant_mimo_test.lti --ticks 1300 --launch --launch-args --mode mpc --model plant_mimo_test.lti --output-count 8 --pairs 1,4 --q-weight 1e12,1e12 --r-weight 100,100 --control-horizon 20 --umin 0 --umax 30 --reference ref_mimo_test.csv
# live-wire 13 s (cpp server, p=2, feature map ABOVE 32 to prove 64-wide plumbing):
.\cpp_controller.exe --mode mpc --model plant_mimo_test.lti --output-count 8 --pairs 1,4 --feature-map 40,33 --q-weight 1e12,1e12 --r-weight 100,100 --control-horizon 20 --umin 0 --umax 30 --target 4e-4,3e-4 --max-packets 1300 --capture capture_mpc_dress64.csv
.\rig\2_loop.ps1 -Run mpcdress64 -RZ2 10.1.0.100 -InputChannels 64 -TickFrames 6 -Ticks 1300 -TimeoutMs 10
```
PASS: 1300/1300; no feature-map fault; `out0` varies; capture `y1..y64`;
server latency CSV written (defaults on now); `offsets=applied` in status.

## S6 — decision + wrap
All green → Thursday runs `RIG_DAY_2026-09-03.md` as written (64-ch, MIMO,
cpp-primary). Anything red on the 64-ch side → fall back to 32-ch single
headstage (yesterday's fully validated config; runbook commands drop to
`-InputChannels 32`). Commit + push; notebook entry; export `.synrig` again if
anything changed.
