# Rig day — 2026-08-29 (SATURDAY, surgical suite, saline): 32-channel recording gates

**Goal:** validate 16 → 32 recording channels (stim stays 8 bipolar pairs) for
Monday's acute experiment. **32-ch is opt-in, not required.** This day ends
green in exactly one of two states: **GO-at-32** or **GO-at-16** — never in
between. `RIG_DAY_2026-08-31.md` stays valid-for-16 at all times; the 32-ch
deltas live at the bottom of this file and are pasted in only on GO-at-32.

**HARD RULE: make the 32-vs-16 decision no later than 3 h before you must
leave.** That protects the ~90 min reversion branch plus slack. Budget: ~4.5 h
ladder + reserve.

**Why re-banking is unavoidable:** the moment the Synapse circuit is edited
(G1), the 08-27 saline gates (delivery + MPC check) are VOID — they validated a
circuit that no longer exists. Saturday must re-bank both under whichever
configuration Monday will run. That is what G5 and G7 are; the reversion branch
re-banks them at 16.

**PC-side code shipped and sim-verified 2026-08-29 morning (no action needed):**
- Loop guard: requesting more channels than the stream carries is a FATAL
  startup error (`--allow-channel-pad` overrides); fewer = warn + truncate.
  Every banner/summary now prints `channelMode=exact|truncate|pad`.
- `4_mpc_server.ps1 -FeatureCount 32` sizes the MPC warm-up so
  `-FeatureChannel` may be 1..32 (the old warm-up capped it at 8 — fixed, bench
  checks 12/13).
- `extract_nthw_templates.py --n-channels 32`; `assess_artifact.py --own-pair
  none`; `check_nnw_mode.py --expect-input 32`; `0_preflight.ps1 -InputChannels
  32`. All defaults remain 16; archived fallback binary =
  `MpcPo8eUdpClosedLoop.aug27-banked.exe`.

Conventions as in `RIG_DAY_2026-08-31.md`: two terminals, `python` = the
PythonIntanAnalysis venv, blocks nest one level, recording started LAST, stim
enable ON immediately after `go`, `-TickFrames 6` everywhere, kill stale
MATLAB between servers.

---

## Gate ladder

### G0 (20 min) — health check at the UNTOUCHED 08-27 config
```powershell
Get-PnpDevice | ? { $_.InstanceId -match 'VEN_4550' } | Format-List FriendlyName, Status, Problem   # want OK
.\rig\0_preflight.ps1          # PASS, dropped=0
.\rig\net_diag.ps1             # PASS
# quick live probe (constant 0, no server): banner must read numChannels=16
.\MpcPo8eUdpClosedLoop.exe 10.1.0.100 . 16 --controller constant --constant-output 0 --skip-udp-send --max-control-ticks 300
```
PASS: all green, banner `numChannels=16 sampleBytes=4`. Establishes "the rig
was fine before we touched anything."

### G0b (10 min) — SNAPSHOT THE SYNAPSE CONFIG. Do not proceed without it.
In Synapse: Save-As a named copy of the exact working experiment/rig config,
e.g. `*_16ch_banked0827`. Then re-open the original and confirm it still loads.
Reversion later is a FILE LOAD, never a re-edit from memory.

### G1 (30 min) — the Synapse edit (the only step that touches the circuit)
- PZ2 bank: detect the 32-ch headstage (the 08-26 16→32 re-detect precedent).
- PO8e streamer tap: widen to 32 channels.
- **Wav1 store: ONE 32-wide store.** Do NOT split 16/16 across Wav1/Wav2 —
  `analyze_quiet_capture.py` and `extract_nthw_templates.py` read Wav1 only.
- Wav1/Wav2 disk saving stays ON.
- Save under a NEW experiment name (the 16-ch snapshot stays untouched).
PASS: circuit compiles and runs Preview.

### G2 (15 min) — card + saving probe
```powershell
.\MpcPo8eUdpClosedLoop.exe 10.1.0.100 . 32 --controller constant --constant-output 0 --skip-udp-send --max-control-ticks 300
# want: Streaming. numChannels=32 sampleBytes=4 (float32), channelMode=exact
```
Then a ~60 s Synapse recording and:
```powershell
python rig\analyze_quiet_capture.py --block <blk>    # want Wav1 (32, T), nonzero
```
If Wav1 shows 16: fix the store in Synapse. Do NOT improvise a Wav1+Wav2
concat path in the suite.

### G3 (30 min) — quiet capture + common-bath validation of the NEW headstage
60 s recording, everything powered, no stim. The 08-26 procedure on 32 ch:
per-channel noise floor (µV std), cross-corr vs the bath-common signal (good
contact ≈ 0.90), 60 Hz phase-lock, feature baseline (volts).
PASS: ≥ ~28/32 channels good; floors in the 8-11 µV regime.
**Write the new 32-ch blacklist table into `LAB_NOTEBOOK_2026-08-29.md`** — the
old ch-13/ch-16 blacklist belongs to the 16-ch headstage and is void here.
Bank: `--baseline` volts for build_touch_reference.

### G4 (30 min) — loop soak at 32
```powershell
# terminal 2:  .\rig\1_server.ps1  (openloop, low/zero amp, capture on)
# terminal 1:
.\rig\2_loop.ps1 -Run soak32 -RZ2 10.1.0.100 -InputChannels 32 -TickFrames 6 -Ticks 6000 -TimeoutMs 10
```
PASS: 6000/6000, `droppedControlTicks=0`, window 6/6/0,
`channelMode=exact(in=32 card=32)`, capture header `y1..y32`, features nonzero
on all non-blacklisted channels.

### G5 (45 min) — DELIVERY RE-BANK under the new circuit
```powershell
.\rig\1c_server.ps1 -Run rnd29 -Schedule random -Ticks 60000     # C++ replay server
.\rig\2_loop.ps1 -Run rnd29 -RZ2 10.1.0.100 -InputChannels 32 -TickFrames 6 -Ticks 60000 -TimeoutMs 10
# recording started LAST; stim enable ON immediately after 'go'
python rig\check_impulse_delivery.py --block <blk> --capture capture_rig_runrnd29.csv --amps 5 10 18 25
```
PASS (same criteria as the void 08-27 gate): wire == design 100% (0 lost, 0
stretched), 0 missed / 0 doubled post-enable, pair map word k → (2k−1, 2k)
exact on all 8 pairs, carrier 6.000 samples/period.

### G6 (20 min) — artifact at 32
```powershell
python rig\assess_artifact.py --block <G5 blk> --own-pair none
```
`--own-pair none` because the 32-ch recording headstage is separate from the
stim electrodes (the legacy 2k−1/2k mask would hide real cortical channels).
PASS: ≤ MODERATE on the verdict → `--feature-trim` stays OFF for Monday.

### G7 (30 min) — 13 s MPC RE-BANK at 32, proving the warm-width fix live
```powershell
# quick fit on the G5 capture (saline => expect the correct REFUSAL / junk fit;
# stage the toy model if needed as on 08-27):
.\rig\3_fit.ps1 -Run rnd29 -Channel <best G5 ch> -Sweep
# then, toy model at slot 9 as on 08-20/08-27:
.\rig\4_mpc_server.ps1 -Target <observed feature range> -Ticks 1300 -FeatureCount 32 -FeatureChannel 20 -Pairs 1 -ModelIndex 9
.\rig\2_loop.ps1 -Run mpc32 -RZ2 10.1.0.100 -InputChannels 32 -TickFrames 6 -Ticks 1300 -TimeoutMs 10
python rig\tracking_metrics.py --capture capture_mpc_<stamp>.csv --ref <ref or target> --y-channels 20 --label mpc32
```
PASS: 1300/1300; NO `BadFeatureChannel`; `policy=fresh` by tick ~100 (08-27
reference: tick 19); `out0` varies; y-verdict = correct saline null; timeouts /
freshTicks within the 08-27 watch band (13% / 97.0%).

### G8 (30 min) — nThw pipeline at 32
One bench thwack block (programmed count known), then:
```powershell
python <NNC>\scripts\touch\extract_nthw_templates.py --block <blk> --site SHAM --day 2026-08-29 --min-pulse-ms 50 --n-channels 32 --force
python rig\prepare_arm_runs.py --channel <a ch > 16> --baseline <G3 volts> ...   # smoke
```
PASS: onset count == programmed count; template npz is (32, 122); smoke prints
tick counts + sham threshold.

### G9 (30 min) — fit-path timing rehearsal on the G5 capture
```powershell
python rig\fit_impulse_model.py --capture capture_rig_runrnd29.csv        # n_ch=32; note the doubled null-FP line
.\rig\3_fit.ps1 -Run rnd29 -Channel <ch> -Sweep                           # TIME the full 32-sweep
```
PASS: sweep + validate + save + export projects inside Monday's 40-min Phase-4
budget (sim reference: 32-sweep on a 3k-tick capture = 1.5 s; the 183k-tick
probe capture is the real test). If it does not fit: bank the fallback —
candidate-guided sweep (fit_impulse_model's top channels + the Phase-2 modal
best channel) instead of a blind 32-sweep.

---

## GO decision

**All of G1-G9 green → Monday = 32-ch.** Paste the delta block below into
`RIG_DAY_2026-08-31.md`, fill in the new blacklist table from G3, commit both
repos, and do not touch the circuit again before Monday.

**Anything unfixably red, or the 3-h cutoff fires → REVERSION BRANCH (~90 min):**

- **R1 (10 min):** load the G0b snapshot in Synapse (file load, zero hand
  edits). Probe: banner `numChannels=16`; 30 s recording → Wav1 `(16, T)`
  nonzero.
- **R2 (40 min):** re-bank DELIVERY at 16 — the G5 procedure and criteria
  verbatim, `-InputChannels 16` (default), match the 08-27 numbers.
- **R3 (25 min):** re-bank the 13 s MPC at 16 — `-FeatureCount 16` (default),
  `-FeatureChannel` ≤ 16, same criteria as 08-27 (slope ≈ 9.06 signature).
- **R4 (15 min):** declare Monday GO-at-16 in the notebook + the 08-31 runbook
  header. The existing ch-13 blacklist applies (16-ch headstage back in).
  **No PC-side code reversion** — every default is 16 and the new guard/fix are
  inert or strictly protective at 16.
- **Do not leave the suite until R2 and R3 are green.** If they fail on the
  bit-identical snapshot config, it is a hardware problem (PnP/PSU/reseat
  ladder), not a migration problem — Monday is NO-GO until it is found.

---

## MONDAY DELTA BLOCK — paste into RIG_DAY_2026-08-31.md ONLY on GO-at-32

- Phase 0: `.\rig\0_preflight.ps1 -InputChannels 32`; banner gate expects
  `numChannels=32`; every Summary must read `channelMode=exact(in=32 card=32)`.
- EVERY `2_loop.ps1` invocation (probing, mpc arm, choi/nnol/nncl loop lines):
  append `-InputChannels 32`. (Forgetting it under-reads 16 with a loud WARN —
  the fits would be valid but half-blind; the Summary channelMode line catches it.)
- Phase 1: replace the ch-13/ch-16 blacklist prose with Saturday's 32-ch
  blacklist table; reference floors/baseline from G3.
- Phase 2: `extract_nthw_templates.py ... --n-channels 32`.
- Phase 4: `assess_artifact.py --own-pair none`; sweep strategy per G9 (full
  32-sweep or candidate-guided); `4_mpc_server.ps1 ... -FeatureCount 32
  -FeatureChannel <fitted ch>` (may be 1..32).
- Phase 5: after NN training,
  `python rig\check_nnw_mode.py models\<inv>.nnw --expect-input 32`.
- Channel policy unchanged: ONE fitted channel for all sites; 32 doubles the
  candidate pool — hold the focal + 15-28 ms latency discipline, since
  per-channel null false positives double (fit_impulse_model prints it).
- Everything else IDENTICAL: stim 8 everywhere (`--udp-output-count 8`,
  `-Outputs 8`, `-Pairs`), `-TickFrames 6`, `-FeatureWindow 6`, 610 Hz locked,
  uMax/safety/enable-gate procedure, arm structure + cut order.
