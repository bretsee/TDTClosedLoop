# Rig day — 2026-08-31 (ACUTE EXPERIMENT, surgical suite)

> **GO.** The 08-26 PO8e blocker is RESOLVED (2026-08-27: the card had unseated
> during the PC's move to the suite — full power-off + reseat fixed it; the rig
> file was never broken). Both deferred saline gates are banked green (below).
> This runbook is the re-dated `RIG_DAY_2026-08-27.md`; that file remains as the
> historical record.

**Gates banked 2026-08-27** (blocks `LD-260827-182650` rndval, `LD-260827-184414`
mpccheck; full numbers in `LAB_NOTEBOOK_2026-08-27.md`):

- **Delivery VERIFIED in the suite**: wire == design 652/652 (0 lost, 0
  stretched); post-enable physical delivery 640/640 single-pulse, 0 missed,
  0 doubled; pair mapping word k → (2k−1, 2k) exact on all 8 pairs.
- **Carrier 101.7253 Hz (base/240) confirmed in the suite circuit**, exactly
  6.000 samples/period → `-FeatureWindow 6` stands.
- **Artifact MODERATE (0 fail / 0 warn)** on controllable channels →
  **`--feature-trim` stays OFF**. Re-run `assess_artifact.py` on the first
  in-vivo probe block before trusting lag-0 content.
- **Safety enable-gate re-confirmed on suite hardware**: the 12 probes sent
  before the stim control was switched on were all blocked (carrier AND Scle
  both gated); 100% delivery after enable. Ctrl+C stim-zero trace fired on
  every exit.
- **13 s MPC check**: 1300/1300 ticks, `policy=fresh` by tick 19, u-on-r slope
  9.06 (matches cl4's 9.7 signature), y-null correct for saline. Watch item:
  timeouts 13% / freshTicks 97.0% (above the 5–11% baseline; MATLAB stalls —
  cpp arms immune).
- **New hardware lesson (in the failure branches now):** "card connects but
  zero frames" or "Found 0 card(s)" ⇒ check `Get-PnpDevice | ? InstanceId
  -match 'VEN_4550'` FIRST. `CM_PROB_PHANTOM` = card off the PCI bus → full
  power-off (PSU cold ~15 s), reseat, boot. Do this before touching Synapse,
  fibers, or the rig file.

---

The one thing today is for: **per-arm tracking of 10 site-specific touch
templates, 100 elicited responses per site** — natural touch recorded with the
new `nThw` pulse, stim driven to reproduce it by four controllers (Choi
open-loop, MPC closed-loop, NN open-loop tape, NN closed-loop), every run scored
minutes after it ends.

The day is **modular**: every phase produces a self-contained banked result.
If time runs out, cut arms from the bottom of the cut order — never cut scoring.

`EXPERIMENT_MANUAL.md` remains the superset reference (failure branches §13,
Choi correspondence §12). Prior gates: `RIG_DAY_2026-08-26.md`,
`LAB_NOTEBOOK_2026-08-27.md`.

**Conventions.** `python` = `C:\Users\brets\Documents\Repositories\PythonIntanAnalysis\.venv\Scripts\python.exe`.
`<NNC>` = `C:\Users\brets\Documents\Repositories\NNController`. Two terminals, both in
`TDTClosedLoop`; MATLAB owns terminal 2. Blocks nest one directory level.
Every server/loop pairing: server `ready` → loop waits at `go` → **START THE
SYNAPSE RECORDING** → type `go` → **switch the stim enable ON immediately**
(anything sent before the enable is silently blocked — 08-27 measured a 5.3 s /
12-probe hole from a late enable). Kill stale MATLAB between servers. Leave
`-TickPhaseUs` unset. `-TickFrames 6` on EVERY capture and deploy (frame-locked
throughout). **2x rate is LOCKED OUT** — skipped 08-26 (no circuit change), the
610 Hz path is the validated one; do not add `-StreamFs` flags.

**Safety standing rules**
- `uMax` is a charge/compliance ceiling, decided BEFORE the animal is on the
  table (probing max = `uMax` for every arm; write it here: ______ µA).
- Stimulator charge: check before the day and before interpreting ANY null
  (the 08-14 dead-battery false-null).
- Ctrl+C zeroes stim (hardware-verified 12 ms). A hard kill BYPASSES it — after
  ANY crash: `python rig\send_envelope.py --kind const --umax 0`.
- Charge ledger per arm comes from the capture u columns (deck script computes it).

## Time budget (~8 h protocol; T0 = rig powered)

| phase | what | budget | banked result |
|---|---|---|---|
| 0 | bring-up + preflight + E2 live packet (pre-animal) | 30 min | rig green in-room |
| 1 | quiet capture → `--baseline` volts, 60 Hz, Wav1/Wav2 saving ON | 15 min | day-of baseline |
| 2 | 10 thwack blocks + immediate nThw extraction each | 90 min | 10 template npz |
| 3 | randomized probing rnd1 (~30 min run) + delivery audit | 40 min | probe capture |
| 4 | fits + go/no-go + gate thresholds + `.lti` export | 40 min | plant model |
| 5 | tapes + manifest + Choi designs (+ NN training starts) | 30 min | 10 refs + designs |
| 6 | ARMS × sites (manifest order), score after every run | ~4.5-6 h | tracking JSON/PNG per run |
| 7 | drift check + wrap-up + push | 30 min | drift result |

**Arm cut order if the day runs long: nncl → nnol → (keep mpc + choi).**
MPC-vs-Choi on the same model is the primary comparison; NN arms extend it.

---

# PHASE 0 — Bring-up + preflight

**FIRST, before anything else (30 s, the 08-27 lesson):**
```powershell
Get-PnpDevice | Where-Object { $_.InstanceId -match 'VEN_4550' } | Format-List FriendlyName, Status, Problem
# Want: Status OK, CM_PROB_NONE. CM_PROB_PHANTOM/absent => full power-off,
# reseat the PO8e card, boot, re-check. Do NOT debug Synapse for this.
```

Then:

```powershell
.\rig\0_preflight.ps1                          # PASS, dropped=0 (also configures this shell)
.\rig\net_diag.ps1                             # PASS (re-check after any re-cabling)
.\rig\6_udp_selftest.ps1 -RZ2 10.1.0.100 -LiveStim   # E2: ONE live data packet, value 0.
#   Run E2 NOW, BEFORE the animal is connected -- never later.
```

Physical: PO8e fiber seated; stimulator charged (log voltage: ______);
IZ2 connected; Synapse tank disk ok (396 GB free 08-26); PCs not sleeping.

### → 0-fail. Any preflight line red
Fix before the animal. `RIG_DAY_2026-07-30.md` branches A1-A4 still apply, plus
the PCI presence check above.

---

# PHASE 1 — Quiet capture (numbers everything downstream needs)

~60 s Synapse recording, everything powered, no stim, no loop.

1. **Open the block and confirm Wav1 is NONZERO on disk** (saving gate — verified
   ON 08-26; re-verify after any Synapse experiment change).
2. Bank three numbers:
   - **baseline** = mean 6-sample MAV feature, volts → `--baseline` for Phase 5. Write it here: ______
   - noise floor per channel (µV std) → template QC context (08-26 suite floor
     was 8-11 µV; ch 13 blacklisted, ch 16 marginal).
   - 60 Hz + harmonics fraction → if it dominates the 15-28 ms band, notch
     FIT-SIDE only (no C++ change on animal day). 08-26 quiet capture: 60 Hz =
     73% of 5-200 Hz relative power (~7 µV absolute).

---

# PHASE 2 — Thwack battery (10 blocks, the controller targets)

For each of the 10 sites — the 9-site battery `D1 D2 D3 D4 P1 P2 P3 LP MP` in
your standard order, plus **SHAM** (thwacker running, same `nThw` output,
touching NOTHING):

1. Record one thwack block (~2 min, aim ≥ 30 contacts; ≥ 15 is the QC floor).
2. IMMEDIATELY extract + QC (terminal 1, while the next site is set up):

```powershell
python <NNC>\scripts\touch\extract_nthw_templates.py `
    --block <block dir> --site D2 --day 2026-08-31 --min-pulse-ms 50
# SHAM add --force (noise-level template is EXPECTED and still written)
# --min-pulse-ms 50: real contacts are ~255 ms machine pulses (validated
#   2026-08-26, 150/150 both test blocks); 50 ms kills switch bounces (one
#   2.7 ms runt seen in test block 1) with zero risk.
```

nThw facts banked 2026-08-26: it is a **float32 STREAM @ 24,414 Hz** (0/1
levels), not an epoc — the extractor detects edges itself. mPos/mCtl do NOT
see this thwacker (flat) — nThw is the only touch record, so per-block QC =
onset count vs the device's programmed count (150 in the test blocks).
**Ch 13 is BLACKLISTED** (bad contact: cross-corr 0.18 vs bath-common 0.90,
38 µV private noise) — if a template's best channel lands on 13, that is the
noise artifact, not a response; ch 16 is marginal (0.81), watch it.

PASS per block = `nThw` onset count ≈ hand count; contact durations real (no
"gap-filled" warning); split-half ≥ 0.5 and a focal best channel (real sites);
SHAM: split-half ≈ 0 and noise-level peak, stated by the tool. Outputs land in
`<NNC>\outputs\BiomimeticInversion\touch\Acute_2026-08-31\` (npz + meta JSON +
summary — these are the archival template artifacts; committed in Phase 7).

Log the best channel per site here — the Phase-4 fit should land on (or near)
the modal one:

| D1 | D2 | D3 | D4 | P1 | P2 | P3 | LP | MP | SHAM |
|----|----|----|----|----|----|----|----|----|------|
|    |    |    |    |    |    |    |    |    |  --  |

### → 2-fail. Template unreliable (split-half < 0.5) on a real site
Re-thwack the site (more contacts) once; if still noisy, note it and continue —
the arm still runs, the result is just weaker for that site.

---

# PHASE 3 — Probing (one randomized run = model data + thresholds)

```powershell
# Terminal 2:
.\rig\1c_server.ps1 -Run rnd1 -Schedule random `
    -PulseAmps 2,4,6,9,13,18,25 -GapTicks 36 -GapJitterMs 60 -Ticks 183000
# adjust -PulseAmps so the ladder straddles the threshold (Choi: 4-10 uA); top = uMax
# Terminal 1 (after 'ready'):
.\rig\2_loop.ps1 -Run rnd1 -RZ2 10.1.0.100 -TimeoutMs 10 -Ticks 183000 -TickFrames 6
# recording LAST, then 'go', then STIM ENABLE ON.  ~30 min, 56 conditions, ~76 trials/condition.
python rig\check_impulse_delivery.py --block <block> --capture capture_rig_runrnd1.csv --amps 2 4 6 9 13 18 25
```

---

# PHASE 4 — Fit + GO/NO-GO

```powershell
python rig\fit_impulse_model.py --input <k>      # per candidate pair; model-free kernels
.\rig\3_fit.ps1 -Run rnd1 -Channel <ch> -Sweep   # then -Validate, then:
.\rig\3_fit.ps1 -Run rnd1 -Channel <ch> -Save    # -> AllModels(10), Ts stamped, offsets stored
matlab -batch "cd('<repo>'); export_plant_lti"   # -> plant_rnd1.lti for the C++/Choi tools
```

- **GO/NO-GO: best-channel `|corr| u→y > 0.1`.** `> 0.9` at lag 0 AND global
  (not focal) = ARTIFACT, not response (real responses peak 15-28 ms) — check
  `assess_artifact.py` before believing it.
- **Bank the printed "output N: range [...]" line.**
- Gate thresholds per pair from the amplitude-response knees (feeds Choi
  synthesis `--gate-threshold`, atten 0.1).
- Bench sanity: `matlab -batch bench_test_reference_mpc` should now be **22/22**
  (the 3 reference-test fails 08-26 were the zero-gain saline fit at slot 10).

### → 4-fail. NO-GO (no channel responds)
Branch per manual §13: check stimulator charge, sSig/UDP1 in the block, pair
choice, then electrode position. Do NOT proceed to arms on a NO-GO — the day
becomes probing + thwack characterization (still a paper's worth of data).

---

# PHASE 5 — Tapes, manifest, designs

```powershell
# 10 per-site reference tapes + seeded randomized site order per arm:
python rig\prepare_arm_runs.py `
    --templates-dir <NNC>\outputs\BiomimeticInversion\touch\Acute_2026-08-31 `
    --channel <fitted ch> --baseline <Phase-1 volts> --seed 20260827 `
    --out-dir day_2026-08-31
# prints per-site tick counts (~22,200 = 3.7 min), peak deltas, the SHAM
# scoring threshold, and the run order per arm -> day_2026-08-31\run_manifest.json
```

- Channel policy: ONE fitted channel for all sites (each site's template row on
  that channel). If the Phase-2 best-channel table disagrees wildly with the
  fitted channel, that is a finding — note it, don't re-plan the day around it.
- Sham tape = the real sham noise template. If its peak delta is non-trivial
  (thwacker vibration coupling), rerun with `--sham-scale 0` (recorded in manifest).
- Scale check: max peak-delta / plant gain must be well inside uMax; if not,
  rerun with `--scale` down and note it.

```powershell
# Choi open-loop designs (one per site; sites from the manifest):
foreach ($s in 'D1','D2','D3','D4','P1','P2','P3','LP','MP','SHAM') {
  python rig\choi_synthesis.py --model plant_rnd1.lti --reference day_2026-08-31\ref_$s.csv `
      --umax <uMax> --mu 1e-9 --lam 0 --gate-threshold <cal> --gate-atten 0.1 `
      --out day_2026-08-31\design_choi_$s.csv --report
}
# READ each report line: the gate must CONVERGE (honest non-convergence = rerun params)
```

**NN training (starts now, runs BETWEEN arms — never during a live loop):**
```powershell
python training\train.py --captures capture_runrnd1.csv --mode inverse `
    --arch residual_mlp --history 25 --umax <uMax> --out models\inv_day.nnw
python training\verify_export.py models\inv_day.nnw
python rig\check_nnw_mode.py models\inv_day.nnw          # MUST print OK
# linear/mlp/residual_mlp ~15 s each; GRU ~50 epochs only (~4-5 min) or skip.
# Optionally add the Phase-6 dense-excitation capture and retrain between arms.
```

---

# PHASE 6 — The arms (the experiment)

Run sites **in the manifest order** for each arm (`run_manifest.json` /
the printed checklist). Per (arm, site): one 22,200-tick run = 100 touch events,
~3.7 min + ~3 min turnaround. Synapse block name mirrors the run label
(`mpc_D2` etc.). **Score immediately after every run** while the next server
starts.

**Arm mpc — MPC closed loop** (MATLAB server; the only correct MPC path for
offset models):
```powershell
# Terminal 2 (kill stale MATLAB first):
.\rig\4_mpc_server.ps1 -Reference day_2026-08-31\ref_<SITE>.csv -RWeight 1e-3 `
    -ControlHorizon 20 -Pairs <k> -FeatureChannel <ch> -ModelIndex 10
# Terminal 1 after 'ready':
.\rig\2_loop.ps1 -Run mpc_<SITE> -RZ2 10.1.0.100 -TimeoutMs 10 -Ticks 22200 -TickFrames 6
# recording LAST, then 'go', then STIM ENABLE ON
```

**Arm choi — Choi open loop** (same plant, offline-optimal tape):
```powershell
# Terminal 2:
.\cpp_controller.exe --mode openloop --play day_2026-08-31\design_choi_<SITE>.csv `
    --output-count 8 --capture day_2026-08-31\capture_choi_<SITE>.csv --max-packets 22200
# Terminal 1: same 2_loop line as above with -Run choi_<SITE>
```

**Arm nnol — NN open-loop tape:**
```powershell
python training\synthesize_openloop_nn.py --nnw models\inv_day.nnw `
    --ref day_2026-08-31\ref_<SITE>.csv --input-channels <ch> --umax <uMax> `
    --out day_2026-08-31\design_nnol_<SITE>.csv --parity     # once per site, pre-generate
.\cpp_controller.exe --mode openloop --play day_2026-08-31\design_nnol_<SITE>.csv `
    --output-count 8 --capture day_2026-08-31\capture_nnol_<SITE>.csv --max-packets 22200
```

**Arm nncl — NN closed loop:**
```powershell
.\cpp_controller.exe --mode nn --model models\inv_day.nnw --output-count 8 `
    --pairs <k> --capture day_2026-08-31\capture_nncl_<SITE>.csv
# SAME --umin/--umax/--max-rate as the nnol tapes or the arms are not comparable
```

**After EVERY run:**
```powershell
python rig\tracking_metrics.py --ref day_2026-08-31\ref_<SITE>.csv `
    --capture <capture csv> --y-channels <ch> --label <arm>_<SITE> `
    --json day_2026-08-31\tracking_<arm>_<SITE>.json --png day_2026-08-31\tracking_<arm>_<SITE>.png
# SHAM runs: add --transient-thresh <value printed by prepare_arm_runs>
# SHAM verdict reads NOT TRACKING with near-zero modulation -- that is the
# CORRECT sham result, not a failure.
```

**Live watch, every run (costs nothing, saves 4 minutes):**
- Within the first ~10 s a `policy=fresh` line must appear (by tick ~100). If
  the loop sits at `policy=zero / no valid reply yet`, Ctrl+C, restart the loop,
  re-record. (One-in-a-few startup race; failure is total and visible
  immediately. 08-27 mpccheck: fresh by tick 19.)
- `out0` must VARY (E1 branch: constant out0 = loop not closed).
- MPC arm: timeouts ~5-13% with freshTicks ≥ 97% is the measured baseline
  (08-27: 13%/97.0%); worry only if freshTicks collapses.

### → 6-fail. MATLAB server stalls mid-tape / loop dies
`python rig\send_envelope.py --kind const --umax 0` (zero the wire), kill both
sides, kill stale MATLAB, re-run the SAME site. staleDropped spikes with held
commands are the 100-ms-stall signature (sal1) — cpp arms are immune.

---

# PHASE 7 — Drift check + wrap-up (do not skip)

1. **Short re-probe** (~5 min): `1c_server -Run rnd2 -Schedule random -Ticks 28000`
   (same seed ladder) → same fit channel still responds? Gain drift %: ______
2. **Re-run ONE early arm/site** (first completed (arm, site)) → compare its two
   tracking JSONs: open-loop degradation vs closed-loop robustness IS the
   value-of-feedback result.
3. Stim zero + disconnect: `python rig\send_envelope.py --kind const --umax 0`.
4. Commit + push BOTH repos (templates in NNController, day_2026-08-31\ +
   captures ledger here). Copy tank block list into the lab notebook.
5. Deck: fill `experiment_manifest` → `python
   scripts\build_experiment_report.py` — every number recomputed from artifacts.

---

*Formats and deeper branches: EXPERIMENT_MANUAL.md §11-13. Scoring thresholds:
TRACKING = best-lag r ≥ 0.5, slope 0.3-1.5, |lag| ≤ 10 ticks.*
